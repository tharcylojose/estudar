/* Esse programa demonstra o uso de comentarios em C,
 * mostrando tanto comentarios em multiplas linhas
 * quanto comentarios de linha unica.
 *
 * Questao 03 - Prog3.c
 ***************************************************************/
#include <stdio.h>  /* Para printf() */
#include <stdlib.h> /* Para system() */

int main() /* Funcao main, ponto de entrada do programa */
{ /* inicio do corpo da funcao main */
    printf("Programa comentado corretamente."); /* Chamada a funcao printf */
    system("PAUSE");                            /* Pausa o console (Windows) */
    return 0;
} /* Fim do corpo da funcao main */
